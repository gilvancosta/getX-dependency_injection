import 'package:flutter/material.dart';

class MinddlewareBindingPage extends StatelessWidget {

  const MinddlewareBindingPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}